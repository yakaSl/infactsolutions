# Settings for Backend (on Cloud Run).
# See https://firebase.google.com/docs/app-hosting/configure#cloud-run
runConfig:
  minInstances: 0
  # maxInstances: 100
  # concurrency: 80
  # cpu: 1
  # memoryMiB: 512

# Environment variables and secrets.
# See https://firebase.google.com/docs/app-hosting/configure#user-defined-environment
#
# Everything below is read server-side only (server actions, the webhook route,
# email + Firestore helpers), so RUNTIME availability is sufficient.
#
# Secret values live in Cloud Secret Manager, created with:
#   firebase apphosting:secrets:set <NAME>
# The `secret:` field is the Secret Manager secret name (kept identical to the
# variable name here). Never put secret values in this file — it is committed to git.
env:
  # --- Non-sensitive config (safe as plain values) ---
  # PayPal base URL controls sandbox vs. live.
  # Sandbox: https://api-m.sandbox.paypal.com  |  Live: https://api-m.paypal.com
  - variable: PAYPAL_BASE_URL
    value: https://api-m.paypal.com
    availability:
      - RUNTIME
  - variable: ADMIN_NOTIFICATION_EMAIL
    value: hello@infactsolutions.net
    availability:
      - RUNTIME
  - variable: RESEND_FROM
    value: Infact Solutions <noreply@infactsolutions.net>
    availability:
      - RUNTIME
  - variable: NEXT_PUBLIC_BASE_URL
    value: https://infactsolutions.net
    availability:
      - RUNTIME

  # --- Firebase Web / Analytics (client-side) ---
  # NEXT_PUBLIC_ vars are inlined into the client bundle at BUILD time, so these
  # MUST include BUILD availability (RUNTIME alone is not enough for the browser).
  # Stored in Cloud Secret Manager (secret names kept identical to the variable
  # names) to keep the raw values out of this committed file. Note: because these
  # are NEXT_PUBLIC_, the resolved values still ship in the client JS bundle.
  # Create/update each with:  firebase apphosting:secrets:set <NAME>
  # Analytics only starts once NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID is present.
  - variable: NEXT_PUBLIC_FIREBASE_API_KEY
    secret: NEXT_PUBLIC_FIREBASE_API_KEY
    availability:
      - BUILD
      - RUNTIME
  - variable: NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN
    secret: NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN
    availability:
      - BUILD
      - RUNTIME
  - variable: NEXT_PUBLIC_FIREBASE_PROJECT_ID
    secret: NEXT_PUBLIC_FIREBASE_PROJECT_ID
    availability:
      - BUILD
      - RUNTIME
  - variable: NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET
    secret: NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET
    availability:
      - BUILD
      - RUNTIME
  - variable: NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID
    secret: NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID
    availability:
      - BUILD
      - RUNTIME
  - variable: NEXT_PUBLIC_FIREBASE_APP_ID
    secret: NEXT_PUBLIC_FIREBASE_APP_ID
    availability:
      - BUILD
      - RUNTIME
  - variable: NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID
    secret: NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID
    availability:
      - BUILD
      - RUNTIME

  # --- Secrets (Cloud Secret Manager) ---
  - variable: PAYPAL_CLIENT_ID
    secret: PAYPAL_CLIENT_ID
    availability:
      - RUNTIME
  - variable: PAYPAL_CLIENT_SECRET
    secret: PAYPAL_CLIENT_SECRET
    availability:
      - RUNTIME
  - variable: PAYPAL_WEBHOOK_ID
    secret: PAYPAL_WEBHOOK_ID
    availability:
      - RUNTIME
  - variable: RESEND_API_KEY
    secret: RESEND_API_KEY
    availability:
      - RUNTIME
  - variable: FIREBASE_SERVICE_ACCOUNT_JSON
    secret: FIREBASE_SERVICE_ACCOUNT_JSON
    availability:
      - RUNTIME
